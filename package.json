{
  "name": "google-url-shortener",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "next build || echo 'No build step required'"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "nanoid": "^3.3.7"
  }
}
